# ATHLETIX

**ATHLETIX** — интеллектуальная платформа для мониторинга физического состояния спортсменов, раннего выявления накопленной усталости и стресса и адаптации тренировочного плана.

Проект разработан в рамках **OVERCLOCK HACKATHON 2026**, трек **BioTech**.

## Проблема

Высокие тренировочные нагрузки без достаточного восстановления могут привести к:

* накоплению усталости;
* повышению уровня стресса;
* снижению качества восстановления;
* перегрузке организма;
* повышению риска травм.

Спортсмену и тренеру важно не только видеть отдельные показатели, но и понимать, когда текущая нагрузка становится слишком высокой.

## Решение

ATHLETIX объединяет показатели состояния спортсмена и анализирует их для оценки текущего уровня нагрузки.

Платформа учитывает:

* частоту сердечных сокращений;
* уровень усталости;
* восстановление;
* тренировочную нагрузку.

На основе этих показателей система рассчитывает условный уровень риска и формирует рекомендацию по тренировке или восстановлению.

## Основные функции

###  Monitoring

Отображение текущих показателей спортсмена и динамики сердечного ритма.

###  AI Coach

Анализ текущего состояния и автоматическое формирование рекомендации.

###  Adaptive Schedule

Изменение тренировочного плана в зависимости от состояния спортсмена.

###  Coach Dashboard

Обзор состояния спортсмена для тренера.

###  History

Просмотр предыдущих показателей и изменений состояния.

## Как работает система

```text
Датчики
   ↓
ESP32
   ↓
Wi-Fi
   ↓
Flask API
   ↓
ATHLETIX
   ↓
Анализ показателей
   ↓
Оценка нагрузки
   ↓
Рекомендация
```

В текущем MVP используются **симулированные данные датчиков**, чтобы продемонстрировать полный цикл работы системы.

Архитектура проекта предусматривает дальнейшее подключение ESP32 и физических датчиков.

## Используемые технологии

* Python
* Flask
* HTML
* CSS
* JavaScript
* Git / GitHub
* ESP32 — планируемая аппаратная интеграция
* MPU6050 — планируемый датчик движения
* Pulse Sensor — планируемый датчик пульса
* BMP280 — планируемый датчик окружающей среды

## Установка и запуск

Чтобы запустить ATHLETIX на своём компьютере:

### 1. Скачать проект

Склонируйте репозиторий ATHLETIX:

```bash
git clone https://github.com/lanswx/athletix.git
```

Затем перейдите в папку проекта:

```bash
cd athletix
```

### 2. Установить необходимые библиотеки

В терминале выполните:

```bash
pip install -r requirements.txt
```

Эта команда установит Flask и другие необходимые зависимости.

### 3. Запустить приложение

Выполните:

```bash
python app.py
```

После запуска Flask запустит локальный сервер.

### 4. Открыть сайт

Откройте браузер и перейдите по адресу:

```text
http://127.0.0.1:5000
```

После этого откроется ATHLETIX.

## Структура проекта

```text
athletix/
│
├── app.py
├── requirements.txt
│
├── templates/
│   └── dashboard.html
│
└── static/
    ├── app.js
    └── style.css
```

## Важное ограничение MVP

Текущая версия использует демонстрационные данные, генерируемые внутри приложения.

Система не является медицинским диагностическим инструментом, а используемая модель оценки риска не является клинически валидированной.

Следующий этап разработки — подключение реальных датчиков через ESP32 и передача данных в платформу по Wi-Fi.
